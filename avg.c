#include<stdio.h>
//#include<conio.h>
int main()
{
int i,arr[10],sum,avg=0;
printf("Enter numbers");
scanf("%d", &arr[i]);
for(i=0;i<10;i++)
{
 sum=sum+arr[i];
} 
avg=sum/10;
printf("average is %d",avg);
return 0;
}